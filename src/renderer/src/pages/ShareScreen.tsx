import styled from 'styled-components';

const ShareScreenWrapper = styled.div`
  flex: 1;
`;

const ShareScreen = () => {
  return <ShareScreenWrapper>ShareFile</ShareScreenWrapper>;
};

export default ShareScreen;
