import styled from 'styled-components';

const ShareFileWrapper = styled.div`
  flex: 1;
`;

const ShareFile = () => {
  return <ShareFileWrapper>ShareFile</ShareFileWrapper>;
};

export default ShareFile;
